{{#camelize}}{{name}}{{/camelize}}PageView = require 'views/{{name}}_page_view'

describe '{{#camelize}}{{name}}{{/camelize}}PageView', ->
  beforeEach ->
    # @model = new {{#camelize}}{{name}}{{/camelize}}()
    @view = new {{#camelize}}{{name}}{{/camelize}}PageView()

  afterEach ->
    # @model.dispose()
    @view.dispose()
